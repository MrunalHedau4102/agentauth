name: Publish to PyPI

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    
    - name: Run tests
      run: pytest tests/ -v
    
    - name: Check versions match
      run: |
        VERSION=$(python -c "import re; content = open('agentauth/__init__.py').read(); match = re.search(r'__version__\s*=\s*[\"\\']([^\"\\']*)[\"\\'']', content); print(match.group(1) if match else '')")
        PYPROJECT=$(python -c "import re; content = open('pyproject.toml').read(); match = re.search(r'version\\s*=\\s*[\"\\']([^\"\\']*)[\"\\'']', content); print(match.group(1) if match else '')")
        echo "Checking: __init__.py=$VERSION, pyproject.toml=$PYPROJECT"
        if [ "$VERSION" != "$PYPROJECT" ]; then
          echo "Version mismatch: __init__.py=$VERSION, pyproject.toml=$PYPROJECT"
          exit 1
        fi
        echo "✓ Versions match: $VERSION"

  build-and-publish:
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install build dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine wheel
    
    - name: Build distributions
      run: |
        python -m build
    
    - name: Check package integrity
      run: |
        twine check dist/*
    
    - name: Publish to PyPI (production)
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        password: ${{ secrets.PYPI_API_TOKEN }}
      if: github.event_name == 'release'
    
    - name: Create GitHub Release Assets
      if: github.event_name == 'release'
      uses: softprops/action-gh-release@v1
      with:
        files: dist/*
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

